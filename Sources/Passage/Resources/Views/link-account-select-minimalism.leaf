<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Link Account</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background: #(theme.colors.background);
            padding: 20px;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .form {
            display: flex;
            flex-direction: column;
            gap: 10px;
            background-color: #(theme.colors.surface);
            padding: 30px;
            width: 450px;
            max-width: 100%;
            border-radius: 20px;
        }

        h1 {
            color: #(theme.colors.onSurface);
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #(theme.colors.onSurfaceVariant);
            font-size: 14px;
            margin-bottom: 15px;
            line-height: 1.5;
        }

        .error {
            padding: 15px;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 10px;
            background-color: #(theme.colors.error);
            color: #(theme.colors.onError);
            border: 1.5px solid #(theme.colors.error);
        }

        .candidate-list {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin: 10px 0;
        }

        .candidate-option {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 15px;
            border: 1.5px solid #(theme.colors.outline);
            border-radius: 10px;
            cursor: pointer;
            transition: 0.2s ease-in-out;
            background-color: #(theme.colors.surface);
        }

        .candidate-option:hover {
            border-color: #(theme.colors.primary);
        }

        .candidate-option:has(input:checked) {
            border-color: #(theme.colors.primary);
            background-color: rgba(0, 0, 0, 0.02);
        }

        .candidate-option input[type="radio"] {
            width: 18px;
            height: 18px;
            cursor: pointer;
            accent-color: #(theme.colors.primary);
        }

        .candidate-info {
            display: flex;
            flex-direction: column;
            gap: 2px;
        }

        .candidate-email {
            color: #(theme.colors.onSurface);
            font-size: 15px;
            font-weight: 500;
        }

        .candidate-phone {
            color: #(theme.colors.onSurfaceVariant);
            font-size: 13px;
        }

        .button-submit {
            margin: 20px 0 10px 0;
            background-color: #(theme.colors.primary);
            border: none;
            color: #(theme.colors.onPrimary);
            font-size: 15px;
            font-weight: 500;
            border-radius: 10px;
            height: 50px;
            width: 100%;
            cursor: pointer;
            transition: 0.2s ease-in-out;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        .button-submit:hover {
            filter: brightness(1.16);
        }

        .button-submit:active {
            filter: brightness(0.82);
        }

        .cancel-link {
            display: block;
            text-align: center;
            color: #(theme.colors.onSurfaceVariant);
            font-size: 14px;
            text-decoration: none;
            margin-top: 5px;
        }

        .cancel-link:hover {
            color: #(theme.colors.primary);
            text-decoration: underline;
        }

        @media (max-width: 480px) {
            .form {
                padding: 25px 20px;
            }

            h1 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <form class="form" method="POST">
        <h1>Link Your #(params.provider) Account</h1>

        <p class="subtitle">
            We found existing accounts that match your profile. Select an account to link with your #(params.provider) login.
        </p>

        #if(params.error):
        <div class="error">#(params.error)</div>
        #endif

        <div class="candidate-list">
            #for(candidate in params.candidates):
            <label class="candidate-option">
                <input type="radio" name="selectedUserId" value="#(candidate.userId)" required>
                <div class="candidate-info">
                    #if(candidate.maskedEmail):
                    <span class="candidate-email">#(candidate.maskedEmail)</span>
                    #endif
                    #if(candidate.maskedPhone):
                    <span class="candidate-phone">#(candidate.maskedPhone)</span>
                    #endif
                </div>
            </label>
            #endfor
        </div>

        <button type="submit" class="button-submit">Continue</button>
        <a href="/" class="cancel-link">Cancel</a>
    </form>
</body>
</html>
