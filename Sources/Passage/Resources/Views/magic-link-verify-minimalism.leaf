<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verifying Sign-In Link</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background: #(theme.colors.background);
            padding: 20px;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            background-color: #(theme.colors.surface);
            padding: 40px;
            width: 450px;
            max-width: 100%;
            border-radius: 20px;
            text-align: center;
        }

        h1 {
            color: #(theme.colors.onSurface);
            font-size: 24px;
            font-weight: 600;
        }

        .error,
        .success {
            padding: 20px;
            border-radius: 10px;
            font-size: 15px;
            font-weight: 500;
        }

        .error {
            background-color: #(theme.colors.error);
            color: #(theme.colors.onError);
            border: 1.5px solid #(theme.colors.error);
        }

        .success {
            background-color: #(theme.colors.success);
            color: #(theme.colors.onSuccess);
            border: 1.5px solid #(theme.colors.success);
        }

        .spinner {
            width: 50px;
            height: 50px;
            margin: 20px auto;
            border: 4px solid #(theme.colors.outline);
            border-top-color: #(theme.colors.primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .btn {
            margin-top: 10px;
            display: inline-block;
            padding: 15px 30px;
            background-color: #(theme.colors.primary);
            border: none;
            color: #(theme.colors.onPrimary);
            font-size: 15px;
            font-weight: 500;
            border-radius: 10px;
            text-decoration: none;
            cursor: pointer;
            transition: 0.2s ease-in-out;
        }

        .btn:hover {
            filter: brightness(1.16);
        }

        .btn:active {
            filter: brightness(0.82);
        }

        @media (max-width: 480px) {
            .container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Verifying Sign-In Link</h1>

        #if(params.success):
        <div class="success">Success! Signing you in...</div>
        <div class="spinner"></div>
        <script>
            setTimeout(function() {
                window.location.href = '#(params.redirectUrl)';
            }, 1500);
        </script>
        #elseif(params.error):
        <div class="error">#(params.error)</div>
        #if(params.loginLink):
        <a href="#(params.loginLink)" class="btn">Return to Login</a>
        #endif
        #else:
        <div class="spinner"></div>
        <p style="color: #(theme.colors.onSurface); font-size: 14px;">Verifying your link...</p>
        #endif
    </div>
</body>
</html>
